<Window x:Class="PL.Call.CallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="callWindow" Height="500" Width="450"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}" Loaded="Window_Loaded" Closed="Window_Closed">

    <!-- Define global styles for controls -->
    <Window.Resources>
        <!-- Global style for labels -->
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Foreground" Value="#0072CE"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <!-- Global style for TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="250" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderBrush" Value="#FF0000"/>
            <Setter Property="BorderThickness" Value="2" />
        </Style>

        <!-- Global style for ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="250" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderBrush" Value="#FF0000"/>
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Foreground" Value="#FF0000"/>
        </Style>

        <!-- Global style for CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderBrush" Value="#FF0000"/>
            <Setter Property="BorderThickness" Value="2" />
        </Style>

        <Style TargetType="Button"
             BasedOn="{StaticResource ButtonStyle}">
        </Style>
    </Window.Resources>

    <!-- Scrollable content area -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <!-- Define grid layout -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <!-- Distance Type row -->
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <!-- Row for Add/Update Button -->
            </Grid.RowDefinitions>

            <!-- Distance Type row -->

            <!-- Id -->
            <Label Grid.Row="0" Grid.Column="0" Content="Id:" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CurrentCall.Id}" />

            <!-- Car Type To Send -->
            <Label Grid.Row="1" Grid.Column="0" Content="Car Type To Send:" />
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}" SelectedItem="{Binding CurrentCall.CarTypeToSend}" />

            <!-- Description -->
            <Label Grid.Row="2" Grid.Column="0" Content="Description:" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentCall.Description, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Address -->
            <Label Grid.Row="3" Grid.Column="0" Content="Address:" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentCall.Address, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Address -->
            <Label Grid.Row="4" Grid.Column="0" Content="Address:" />
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentCall.Address, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Address -->
            <Label Grid.Row="5" Grid.Column="0" Content="Address:" />
            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentCall.Address, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Latitude -->
            <Label Grid.Row="6" Grid.Column="0" Content="Latitude:" />
            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding CurrentCall.Latitude, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Longitude -->
            <Label Grid.Row="7" Grid.Column="0" Content="Longitude:" />
            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding CurrentCall.Longitude, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Open Time -->
            <Label Grid.Row="8" Grid.Column="0" Content="Open Time:" />
            <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding CurrentCall.OpenTime, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Max Time -->
            <Label Grid.Row="9" Grid.Column="0" Content="Max Time:" />
            <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding CurrentCall.MaxTime, Mode=TwoWay, NotifyOnValidationError=true,  ValidatesOnExceptions=true}" />

            <!-- Status -->
            <Label Grid.Row="11" Grid.Column="0" Content="Status:" />
            <ComboBox Grid.Row="11" Grid.Column="1" ItemsSource="{Binding Source={StaticResource CallStatusCollectionKey}}" SelectedItem="{Binding CurrentCall.Status}" />

            <!-- Add/Update Button -->
            <Button Grid.Row="12" Grid.Column="0" Grid.ColumnSpan="2" 
                    Content="{Binding ButtonText}" 
                    Click="btnAddUpdate_Click" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Width="100" Height="50" 
                    Margin="10" />
        </Grid>
    </ScrollViewer>
</Window>
